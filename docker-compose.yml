services:
  # MongoDB with Replica Set support (required for transactions)
  mongodb:
    image: mongo:7
    container_name: charity-hub-mongo
    ports:
      - "27017:27017"
    env_file:
      - .env
    command: [ "--replSet", "rs0", "--bind_ip_all", "--keyFile", "/etc/mongo-keyfile" ]
    volumes:
      - mongodb_data:/data/db
      - ./mongo-keyfile:/etc/mongo-keyfile:ro
    healthcheck:
      test: mongosh --eval "rs.status()" || exit 1
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  mongodb_data:

    # Setup:
    # 1. Copy environment file:
    #    cp .env.example .env
    #    # Edit .env and change passwords
    #
    # 2. Create keyfile (run once):
    #    openssl rand -base64 756 > mongo-keyfile
    #    chmod 400 mongo-keyfile
    #
    # 3. Start container:
    #    docker-compose up -d
    #
    # 4. Initialize replica set:
    #    docker exec -it charity-hub-mongo mongosh -u admin -p YOUR_PASSWORD --authenticationDatabase admin --eval "rs.initiate()"
